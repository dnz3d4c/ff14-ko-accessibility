# upstream 클론은 버전 관리 밖에 둔다.
# 읽기 전용 참조이며, 채택 시 submodule로 전환한다 (docs/ko-client-port-feasibility.md §9).
vendor/

# .NET
bin/
obj/
*.user

# 커밋 검증기 (파이썬)
__pycache__/
.pytest_cache/

# 배포 산출물 (run\pack.bat이 만든다). 162MB EXE라 저장소에 넣지 않는다.
dist/
