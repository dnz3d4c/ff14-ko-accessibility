# 공식 가이드 코퍼스 — 무엇을 받아 두고 어떻게 쓰나

- 출처: [파이널판타지14 공식 가이드](https://guide.ff14.co.kr/lodestone/playguide) (액토즈소프트)
- 받은 판: 7.55 / 2026. 08. 04 · 받은 날: 2026-08-18
- 쓰는 규칙은 [ko-user-guide 스킬](../.claude/skills/ko-user-guide/SKILL.md)이 갖는다. 여기는 **무엇이 있고 어떻게 다시 뽑나**만 갖는다

## 1. 왜 받아 두나

우리 사용자 문서는 **공식 가이드와 같은 모양**으로 쓰기로 했다. 그런데 "공식 가이드 형식"이 무엇인지를 기억으로 적으면 확인한 것과 그럴듯한 것이 섞인다 — 이 저장소는 `Aetheryte → 에테라이트`로 그걸 한 번 겪었다([ko-localization 스킬](../.claude/skills/ko-localization/SKILL.md) §3).

그래서 원문을 받아 두고 **거기서 세서** 규칙을 적는다. 쓰임은 둘이다.

- **형식·문체의 근거** — 종결어미, 제목 번호, UI 경로 표기를 원문에서 센다
- **이름 찾기** — 게임 화면 문자열은 `overlay/ko/terms.json`(Addon 시트)이 먼저고, 메뉴 경로·기능 이름처럼 시트에 없는 말은 여기서 나온다

## 2. 무엇이 들어 있나

초보자 가이드 아래 **문서 62건을 받아** 둔다. 여기가 문체의 근거다.

거기에 **대분류 랜딩 12건**(딥 던전·하우징 같은 것)을 더 받되 **문체의 근거로는 안 쓴다** — 홍보문이라 문장이 다르다. 용어를 찾을 때만 본다.

### 문서 62건

| 대분류 | 문서 (`번호` 제목) |
|--------|--------------------|
| 기초 가이드 | `107` 회원가입 · `106` 보안서비스 · `102` 설치/실행 · `103` 캐릭터 생성 (이상 게임시작) · `104` 조작법 · `112` 인터페이스/HUD · `113` 퀘스트 · `114` 탈것 · `115` 비행&잠수 · `116` 초코보 수송 · `117` 비공정 (이상 이동) · `105` 초보자의 집 · `119` 모험록 |
| 캐릭터 & 육성 | `120` 장비 착용 · `121` 장비 수리 · `122` 마테리아 (이상 장비) · `123` 토벌수첩 · `124` 공략수첩 · `125` 탐험수첩 (이상 수첩) · `126` 길드 의뢰 · `127` 버디 · `128` 우호부족 · `129` 알라그 석판 · `130` 총사령부 |
| 전투 콘텐츠 | `131` 임무 찾기 이용 · `132` 임무 지원 · `133` 트러스트 · `134` 돌발 임무 (이상 임무 찾기) · `135` 마물 사냥 · `136` 특수 던전 · `137` 금단의 땅 에우레카 · `138` 세이브 더 퀸 · `165` 초승달 섬 (이상 특수 필드) |
| 생활 콘텐츠 | `139` 소장품 · `140` 단골 거래 · `141` 먼바다 낚시 · `142` 아이템 분해 |
| 부가 콘텐츠 | `143` 보물찾기 · `144` 무기 강화 |
| 커뮤니티 | `145` 초보자 채널 · `146` 파티 찾기 · `147` 자유부대 · `148` 링크셸 · `149` 감정 표현 · `150` 동아리 · `166` 대화 말풍선 · `168` 전략 게시판 |
| 편의 기능 | `151` 집사 · `152` 장터 · `153` 여관 · `154` 장비 투영 · `155` 아이템 염색 · `156` 미용 · `157` 서버 텔레포 · `158` 유실물 찾기 · `159` 우편(모그레터) · `167` 빠른 메뉴 |
| 기타 | `160` 데이터 백업/복원 · `161` 모험가 카드 & 초상화 · `162` 꼬마 친구 · `163` 스크린샷 · `164` 매크로/명령어 |

번호가 곧 주소다 — `https://guide.ff14.co.kr/lodestone/playguide/view/112`.

**우리한테 특히 쓸모 있는 넷.** `104` 조작법(키 이름과 기본 바인딩 표), `112` 인터페이스/HUD(화면 구성 요소 이름), `164` 매크로/명령어(텍스트 명령어 표기), `167` 빠른 메뉴(입력 수단별로 가르는 형식).

### 대분류 랜딩

`deepdungeon`·`deepdungeon2`·`deepdungeon3`·`deepdungeon4` (딥 던전 넷) · `housing` 하우징 · `goldsaucer` 맨더빌 골드 소서 · `islandsanctuary` 무인도 개척 · `cosmic` 우주 개척 · `ishgard` 이슈가르드 부흥 · `job` 전투 직업 가이드 · `job-craft` 생활 직업 가이드 · `map` 풍맥 가이드

## 3. 어떻게 쓰나

    run\guide.bat fetch        인덱스와 문서 전량 (1초 간격, 캐시 있으면 건너뜀)
    run\guide.bat md           캐시를 마크다운으로 다시 옮긴다 (네트워크 없음)
    run\guide.bat scan         시각 의존 표현이 어디에 몇 건인가
    run\guide.bat find 소지품  코퍼스에서 낱말 찾기

받은 것은 `tools/ko-guide/out/`에 있다. `html/`이 원문이고 `md/`가 정규화한 것이다.

**`find`가 이름 찾기의 2순위 창구다.** 1순위는 `run\terms.bat`(게임 데이터)이고, 순서와 경계는 [ko-user-guide 스킬 §6](../.claude/skills/ko-user-guide/SKILL.md).

## 4. 저장소에 원문을 넣지 않는다

`tools/ko-guide/out/`은 `.gitignore`에 있다. 액토즈/스퀘어에닉스의 문서를 통째로 재배포하지 않는다 — `tools/ko-terms`가 게임 텍스트 19,592행을 안 넣는 것과 같은 규약이다.

저장소에 남는 것은 셋이다.

| 무엇 | 어디 | 성격 |
|------|------|------|
| 문서 대장 | `tools/ko-guide/corpus.json` | 무엇을 어디서 받았나 + 원문에서 센 통계 |
| 인용 대장 | `overlay/ko/guide-quotes.json` | **우리가 실제로 인용한 문장.** 지금 12줄이다 |
| 규칙 | `.claude/skills/ko-user-guide/SKILL.md` | 그 인용에서 뽑은 형식 규약 |

## 5. 무엇이 검사되나

`tools/ko-guide/tests`가 pytest에서 돈다(`run\check.bat` 1단계).

- **파서** — 제목 계층·줄바꿈·UI 경로 표기(`[A > B]`)가 정규화에서 안 뭉개지는가. 늘 돈다
- **대장 구조** — 문서 번호가 겹치지 않는가, 줄마다 출처가 있는가. 늘 돈다
- **인용이 원문에 실제로 있는가** — 캐시가 있을 때만. 스킬이 공식 가이드를 인용하는데 그 문장이 없으면 지어낸 것과 같다
- **통계가 원문과 맞는가** — 캐시가 있을 때만. 정규화를 고쳐 놓고 대장을 안 고치는 것을 잡는다

그리고 `tools/docs-check`가 **스킬과 이 문서가 손으로 옮겨 적은 숫자**를 대장과 대조한다. 시각 의존 표현은 `scan`이 229곳을 센다.

**가이드가 개정되면 숫자가 움직인다.** `fetch --refresh` 뒤에 빨개지는 것이 정상이다 — 값을 맞추고, 문체가 실제로 바뀌었으면 스킬을 고친다.
